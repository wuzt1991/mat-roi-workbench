(function(root){
'use strict';
const MAX=1e12, finite=n=>typeof n==='number'&&Number.isFinite(n)&&Math.abs(n)<=MAX;
function fraction(n){if(!finite(n))throw Error('无效数值');let s=String(n),sign=1n;if(s[0]==='-'){sign=-1n;s=s.slice(1);}const [raw,exp='0']=s.toLowerCase().split('e'),[whole,dec='']=raw.split('.'),power=Number(exp)-dec.length;return power>=0?{n:sign*BigInt(whole+dec)*10n**BigInt(power),d:1n}:{n:sign*BigInt(whole+dec),d:10n**BigInt(-power)};}
function decimal(n,places){try{const f=fraction(n);return (f.n*10n**BigInt(places))%f.d===0n;}catch{return false;}}
function areaFraction(size){const w=fraction(size.irregular?size.productionW:size.salesW),h=fraction(size.irregular?size.productionH:size.salesH);return {n:w.n*h.n,d:w.d*h.d*10000n};}
function errors(s){const out=[],pct=n=>finite(n)&&n>=0&&n<100&&decimal(n,4),area=n=>finite(n)&&n>0&&n<=10000&&decimal(n,6);if(!s||!['uniform','rank','area'].includes(s.type))return ['请选择有效定价策略类型'];if(s.type==='uniform'&&!pct(s.margin))out.push('毛利率需为 0 至小于 100%，最多四位小数');if(s.type==='rank'&&(!Array.isArray(s.tiers)||!s.tiers.length||s.tiers.length>100||!s.tiers.every(pct)||!pct(s.fallback)))out.push('阶梯毛利需配置 1–100 档及有效后续毛利率');if(s.type==='area'&&(!area(s.baseArea)||!area(s.stepArea)||!pct(s.baseMargin)||!pct(s.cap)||s.cap<s.baseMargin||!finite(s.stepPoints)||s.stepPoints<0||s.stepPoints>100||!decimal(s.stepPoints,4)))out.push('请检查面积、递增百分点和毛利上限');return out;}
function target(s,row,rows){if(errors(s).length)throw Error(errors(s)[0]);if(s.type==='uniform')return s.margin;if(s.type==='rank'){if(rows.some(r=>!finite(r.pricingCost)||r.pricingCost<0||r.costError))throw Error('存在成本未确认的 SKU，阶梯策略暂停');const ranks=[...new Set(rows.map(r=>Math.round(r.pricingCost*100)))].sort((a,b)=>a-b),i=ranks.indexOf(Math.round(row.pricingCost*100));return s.tiers[i]??s.fallback;}const a=row.size?areaFraction(row.size):fraction(row.area),b=fraction(s.baseArea),step=fraction(s.stepArea),n=a.n*b.d-b.n*a.d;const count=n<=0n?0n:(n*step.d)/(a.d*b.d*step.n),base=fraction(s.baseMargin),inc=fraction(s.stepPoints),cap=fraction(s.cap),valueN=base.n*inc.d+count*inc.n*base.d,valueD=base.d*inc.d;if(valueN*cap.d>=cap.n*valueD)return s.cap;return Number(valueN)/Number(valueD);}
function quote(cost,margin,fee=0,tax=0){const denominator=1-(margin+fee+tax)/100;if(![cost,margin,fee,tax].every(finite)||cost<=0||margin<0||margin>=100||fee<0||tax<0||denominator<=0)return {price:null,error:cost===0?'零成本无法反推正售价，请手动输入':'成本或毛利与费率组合不可定价'};const raw=cost/denominator;if(!finite(raw)||raw<=0)return {price:null,error:'策略售价超出范围'};let cents=Math.ceil(raw*100),price=cents/100;const meets=x=>x*denominator>=cost-Math.max(1,cost)*1e-12;if(cents>1&&meets((cents-1)/100))cents--;price=cents/100;if(!meets(price)){cents++;price=cents/100;}if(!finite(price)||price<=0||!meets(price))return {price:null,error:'策略售价核验失败'};return {price,margin:actualMargin(cost,price,fee,tax),targetMargin:margin};}
function actualMargin(cost,price,fee=0,tax=0){return finite(cost)&&finite(price)&&price>0?(1-cost/price-(fee+tax)/100)*100:null;}
function effectivePrice(item,strategy,row,rows,params={}){if(item.priceMode==='manual'||!strategy)return {price:item.price,targetMargin:null};try{return quote(row.pricingCost,target(strategy,row,rows),params.fee,params.tax);}catch(e){return {price:null,error:e.message};}}
const api={fraction,decimal,areaFraction,errors,valid:s=>errors(s).length===0,target,quote,actualMargin,effectivePrice};if(typeof module==='object')module.exports=api;else root.PricingRules=api;
})(typeof globalThis==='object'?globalThis:{});
