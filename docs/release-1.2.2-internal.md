# 地垫工作台 1.2.2 本地内测

日期：2026-09-21。施工清单已修订并接入本版；Windows 安装包已生成，本机 Mac 已完成备份、替换并启动。本次不推送、不上传、不在线发布。

商品转表按最新标准统一 29 列表头，主条码和规格辅助码留空，支持拖入文件；修复少于模板行数时可能出现的 G62 共享公式错误。销售报表按相同尺寸合并不同图案，复核后自动回填销量与订单占比，保留拖放、售价和原编号，支持撤销。默认成交订单数，件数需明确确认。

包含完整施工功能：可复用规则与多套定价、规格策略/手动价格保护、尺寸组合、上架价格计算器、计划经典列表与置顶、统一生产尺寸、实际到手销售金额、旧账升级兼容、大表流式处理和双主题。右上角加入总动效开关，遵循系统减少动态。Windows 构建与运行时内置国内 OSS 更新地址。

本次审查修复：销售数量格式误读、关闭未应用复核后退出保护遗漏、迟到异步结果污染新对话框、排除组数丢失、同一导入编号内容变化误报成功、更新失败原因被通用文案覆盖。

## 实际交付

- Windows x64 NSIS 安装包：`/Users/wuzt/Documents/ChatGPT/爆款复刻-地垫 001/outputs/地垫工作台-1.2.2-内测/地垫工作台-1.2.2-windows-x64.exe`，103,073,919 字节。
- SHA-256：`3eaf5da5eab04653819e845f87f4022f32f2f764cd2075e0e4359952fa59f666`。同目录 `SHA256.txt` 可核对。
- 本机应用：`/Applications/地垫工作台.app`，已启动并显示“内测版 v1.2.2”。业务数据仍在原目录，1 个店铺、3 个计划、16 条规格、3 条历史账目及完整工作区均与退出前深度一致。
- 回退备份：`/Users/wuzt/Documents/ChatGPT/爆款复刻-地垫 001/outputs/mac-local-1.2.2-20260921/backup`，包含原应用、业务库/文件会话、桌面配置/草稿及退出时数据。备份通过 SQLite 完整性校验，有文件 SHA-256 清单；这些私人资料未进入安装包或版本库。
- 源码工作树：`/Users/wuzt/Documents/ChatGPT/爆款复刻-地垫 001/roi-workbench-construction-20260919`。未提交、未推送、未上传或在线发布；主工作树只同步本次施工与交付文档。

## 审查和验证

- `npm test`：395/395 通过；语法、运行文件清单、差异空白检查通过。新增错误场景先复现失败再修复。
- 最终 Mac 包的子进程和签名检查通过；两个平台最终包都通过运行资源白名单和源文件一致性检查。未包含个人 Excel、业务库、原型或发布凭据。
- 从 Windows NSIS 安装程序中实际解出 `app.asar`，与打包目录完全一致；EXE SHA-512/大小与 latest.yml 一致，内置更新源为 OSS。Windows 安装包内代码在 Mac Electron 上复验，不能等同于 Windows 原生运行。
- 原始销售表在最终 Mac 包上完成真实浏览器拖入、选择文件、识别、汇总、回填、保存刷新及撤销。19 条记录合为 5 个尺寸，共 488 单，占比 48.77%、27.46%、17.42%、3.69%、2.66%；售价和原编号不变。
- 动效开关、刷新后偏好、系统减少动态及日夜主题在 1440/842/390 宽度通过；没有页面脚本错误或页面横向溢出。
- 两个最终包内代码均通过真实 60 行《华住 9.18.xlsx》及 1/60/99/100/139 边界导出回读；固定 29 列、P/Q 空白、文本 ID、样式及非工作表模板部件保留，无残留公式。测试中的厚度选择只在隔离测试使用，原文件未修改。
- 用户真实格式 3 旧备份通过导入、SQLite 迁移、原文恢复点、重开验证，70 条规格及 11 条混合年代历史账目保持完整。
- 最终 Mac 包执行 500000 行全链路：导入 56.1 秒、导出 11.1 秒、深分页 p95 14.1 ms、峰值 RSS 496 MiB；全量逐行回读哈希一致；单条/全组修改与撤销通过。500001 行以 ROW_LIMIT 拒绝。
- 实际 35 MiB 旺店通多工作表源流式读取第一表 271726 条业务行，23.6 秒、246 MiB；原始文件不变，计数、SQL 分页、重开通过。该源有重复表头，商品模式正确要求确认字段；本轮真实大表仅验证解析层，没有自动选择歧义字段或应用销售数据。完整导出链路使用上面的 50 万行合成数据验证。

证据目录：`/Users/wuzt/Documents/ChatGPT/爆款复刻-地垫 001/outputs/release-1.2.2-20260921`；本机替换证据：`/Users/wuzt/Documents/ChatGPT/爆款复刻-地垫 001/outputs/mac-local-1.2.2-20260921/verification.json`。

## 尚未执行的正式发布验收

Windows 原生安装、旧版覆盖及跨版本 OSS 更新，Windows x64/8 GiB/SSD 性能、Excel/WPS 打开确认仍待该平台验证。Windows 未使用受信任发布证书，本轮不宣称签名发布。远端最小权限凭据与 CI Secrets 未配置，OSS 未上传 1.2.2；后续发布工作流仅在本地修订并做模拟传输测试。

旧版 Windows 用户首次切换更新源需要手动覆盖安装此新版，数据目录保留。macOS 自动更新仍不支持。本次是本地内测交付，不能将这些尚未执行的正式发布步骤标为完成。
