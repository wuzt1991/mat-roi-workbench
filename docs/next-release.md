# 内测版施工清单：1.2.10 使用中／1.2.13 结构重构候选

更新：2026-09-23。以用户最新确认的顺序为准：先导入识别本表材质，再统一材质与厚度，最后确认导出；正常表格完成第二步即可导出。1.2.8 的导入前预设、自动补充弹窗、满屏下拉框和批量工具已经被本轮设计替代，历史记录另存为 `product-flow-superseded-127-128.md`。

## 1.2.13 阶段 4（本地候选，未安装）

- [x] 删除废弃商品转表回退界面、旧趋势图页面、无调用包装、关联事件和样式；保留有依赖的历史模型。
- [x] 商品模块不可用时给出明确提示、保存保护下重新加载和备份入口；正式流程保持。
- [x] 运行资源、版本、依赖快照及许可证纳入统一白名单，Mac 与 Windows 本地候选完成构建与载荷核对。
- [x] 462 项自动测试、语法和清单；23 组有效页面／弹窗与上一阶段相同，计算和备份内容一致。
- [x] 实际 Mac 程序完成隔离升级、保存、导出、替换恢复及旧版 1.2.10 回读最新数据库。
- [x] 最终 Mac 候选完成销售／入账／备份和商品全流程回归；两主题 × 四窗口滚动通过，最终 ZIP/EXE 哈希与证据对应，中间展开副本已清理。
- [ ] Windows 原生安装／升级、Excel/WPS 人工验收及 8 GiB Windows 机器验证未执行。
- [x] 不替换本机、不推送、不发布；真实业务数据库未用于测试。

详见 [阶段 4 记录](refactor-stage4.md) 与 [维护入口](refactor-maintenance.md)。下方为历史记录，不代表当前候选验收。

## 1.2.12 后续结构重构（本地候选，未安装）

- [x] 阶段 3：外壳挂载／退出保护、规则校验与视图、销售流程／模型／拖放、冻结入账与明细、总账共用查询及筛选状态、版本兼容入口。
- [x] 保持单一保存队列、手动价格保护、完整销售分母、尺寸暂存、历史快照、旧备份与现有 UI 步骤。
- [x] 461 项测试、语法和运行清单通过；23 组页面／弹窗与基线一致，计算和备份表内容相同。
- [x] 实际浏览器与 Mac 候选窗口验证销售保存失败重试、撤销、新尺寸；入账重试、更正、总账、Excel 导出／恢复及刷新读取。
- [x] 原施工树及本机 1.2.10 应用载荷不变；无推送或发布。
- 阶段 4 清理和当前平台状态由上方 1.2.13 条目替代；1.2.12 本身未做 Windows 原生验收。

详见 [阶段 3 实施与验收记录](refactor-stage3.md)。下面保留早期候选与正式交付的历史记录，不作为本次成品验收证据。

## 1.2.11 首轮结构重构（本地候选，未安装）

- [x] 审核并修订结构重构参考方案，固定含原未提交成果的 1.2.10 完整基线，在独立工作树实施阶段 0～2。
- [x] 商品转表拆分控制器、纯视图、模型辅助和命令核对；保留当前导入、材质厚度、商品展开与批量、拖入、搜索和导出流程。
- [x] 响应丢失先查回执；撤销与异步重算补齐原子回执；销毁和迟到响应隔离；摘要与 SKU 同修订号发布，读失败保留视图并阻止基于旧列表写入。
- [x] 最终 452 项测试、语法及运行清单、Mac 成品载荷与子进程、真实窗口、包内新旧数据对照、三种分组性能、100 轮 4 并发零失败通过。
- [x] 包内 500000 行全量修改／撤销／导出回读哈希一致，500001 行正确拒绝；验证进程峰值 386.34 MiB。
- [x] 本机安装仍为用户接受的 1.2.10；原施工源和现有应用载荷不变，真实数据未写入。本轮没有远端推送或发布。
- [ ] 1.2.11 Windows 构建与原生安装升级未执行；本轮不替换本机，不声称完成全应用阶段 3～4。

详见 [首轮施工与验收记录](refactor-product-result.md)。原 1.2.10 Windows 交付与历史记录如下，不能代替 1.2.11 的平台验收。

## 1.2.10 商品分组增量（已安装本机）

- [x] 手动调整按平台＋店铺＋商品 ID 显示商品；同名不同链接分开，缺少身份的行仍隔离，不按名称猜测合并。
- [x] 默认收起 SKU，每页 20 个商品；展开后查看、分页和单独修改此商品的 SKU。单商品每页最多 100 条，商品不被规格分页拆散。
- [x] 每个商品提供“统一修改”：保留材质仅改厚度，或同时改材质和对应厚度。确认页明确作用于全部 SKU，包括未展开、未匹配搜索和其他 SKU 页；保留尺寸等其他字段。
- [x] 整商品修改使用原有事务与版本校验，支持撤销；保存失败保留输入。修改后商品摘要即时更新，导出摘要按实际厚度显示，混用厚度不再显示旧预设。
- [x] 全表商品名／ID／SKU／规格搜索保留；单商品搜索命中自动展开结果，不改变批量范围或导出范围。
- [x] 437 项测试、语法和运行清单检查通过。实际 Mac 成品验证 120 SKU 跨页、同名不同链接／店铺隔离、单条厚度例外、批量材质／厚度、失败重试、撤销、搜索后批量、导出逐行成本及原尺寸、重启恢复、两主题四宽度展开和滚动；导入拖放及等待动画保留。
- [x] 本机已更新 1.2.10：备份并校验 156 个数据／配置文件，11 个数据库完整性通过；业务工作区与 10 个既有数据库不变。启动检查期间同源 334 行商品表重新导入为新会话，除会话 ID 和创建时间外内容一致，保留当前操作状态。
- [x] 旧 1.2.9 复用已验证的 ZIP 归档；构建应用已移入安装目录，没有额外展开副本。
- [x] 用户已确认本机界面并要求打包；Windows x64 NSIS 安装包已生成，待交付文件与测试候选 SHA-256 一致。
- [x] 干净依赖安装发现 minimist 构建锁记录被版本升级误改为不存在的 1.2.9；已恢复原 1.2.8 元数据并验证安装通过。应用运行文件、生产依赖和已确认的 Mac 成品保持一致。
- [x] 同一交付 EXE 的 Windows 原生测试全部通过：旧版 1.1.10 自动重启升级并保留数据、实际窗口商品分组／整商品修改／搜索／导出／滚动、100 轮并发零失败、500000 行全量修改撤销及导出回读哈希一致、500001 行超限拒绝、另一套 Windows 连续三次覆盖安装及启动。
- [x] Windows 1.2.10 安装包及说明已交付，最终文件与受测候选逐字节一致；本机保留用户已确认的 1.2.10。仅推送隔离测试分支，没有创建 Release、上传 OSS 或改动在线更新。详见 [1.2.10 内测交付记录](release-1.2.10-internal.md)。

证据：`outputs/release-1.2.10-20260923/`、`outputs/mac-local-1.2.10-20260923/`。

## 1.2.9 基础施工与验收

- [x] 三个独立步骤：导入表格 → 材质与厚度 → 确认导出。第一步只提供文件拖入与选择，不再出现预设管理。
- [x] 第二步只列出本表使用的材质；每种材质只选一次厚度，记住上次选择用于下一份表的预选，必须点击确认才应用。
- [x] 材质识别检查商品名与规格原文；“冰丝水洗底”等包含关系采用具体名称，真正多材质／伪亚麻等歧义不猜测。
- [x] 未识别／多材质商品在同一步确认，可统一指定材质或分商品指定，再使用对应材质的统一厚度。复杂个例可明确选稍后手动处理。
- [x] 确认后覆盖对应材质的原表厚度和原厚度冲突；显式再次统一也覆盖旧单独厚度。单条修正在确认之后保留，普通重新计算不覆盖单条例外。
- [x] 材质指定、厚度覆盖、人工复核更新和结果代次原子发布；取消／失败保留原会话和输入，禁止发布半套结果。
- [x] 第三步以结果摘要及导出按钮为主；不自动弹出待处理面板。只有特殊尺寸、原表字段缺失等复杂情况才进入手动调整。
- [x] 手动调整重做为可搜索、分页的商品摘要列表；点击修改才打开一条规格的材质、厚度与长宽输入，删除旧复核表格、逐行下拉框与批量操作栏。
- [x] 搜索商品名／商品 ID／SKU／规格覆盖整表，搜索和分页不缩小导出范围；新流程未确认统一厚度前后端均阻止导出。
- [x] 原表身份字符串、零库存、29 列最新模板及留空条码保持正确。原始商品名称与规格文字保留。
- [x] 源码实景验证正常 140 条表、同一步补材质、分别指定材质、原厚度覆盖、失败重试、跨页搜索单条修正、复杂尺寸、列歧义、多工作表、拖放、刷新恢复、等待动画与两主题四宽度滚动。
- [x] 实际 Mac 成品流程复验、载荷审计和签名检查通过。
- [x] 本机已备份并替换为 1.2.9，确认启动及 334 条商品会话恢复；业务工作区和原始行保留。旧版和候选已校验归档，应用入口仅保留当前安装版。
- [x] 此基础流程已随用户确认的 1.2.10 完成 Windows 构建、原生测试与交付；不另行生成 1.2.9 Windows 包，不发布在线更新。

## 本机交付收尾规则（2026-09-22 补充）

- 完成交付后，应用入口只保留 `/Applications/地垫工作台.app`。Mac 打包输出、失败候选和旧版回退副本不得长期以展开的 `.app` 留在项目目录，否则 macOS 会将它们收进应用列表。
- 旧应用用 `ditto` 压缩成 ZIP，存入 `outputs/历史应用归档.noindex/`。先逐文件验证归档，再移除展开副本；完全相同的应用合并为一份 ZIP，在原位置写恢复路径说明。
- 对移走的旧路径逐项取消 Launch Services 登记，保留当前安装路径登记。不要重置整个系统应用数据库或用户图标排列。
- 业务数据、数据备份、源码、测试证据和最新 Windows 安装包保留。收尾核验当前应用及业务数据未被改变。

## 1.2.6 最终验收

- [x] 原版 React Bits Lattice Loader 等待动画；保留拖入、取消、计时、阶段提示与动效偏好；慢首帧、轮询重绘、切页后连续播放。
- [x] SQLite 首次访问前锁等待及失败关闭；IPC 完成回传和任务占用发布时序修复。
- [x] Electron 42.11.6 官方 Windows 退出崩溃修复。
- [x] 文件名尚为空时登记恢复会话；取得文件名后正常保存，真实窗口刷新／重启恢复通过。
- [x] 源码 420 项测试、两平台成品审计、Mac 14 项专项与两平台各 100 轮并发通过。
- [x] 同一交付 EXE 的 Windows 原生安装、1.1.10 自动重启升级及数据保留、两主题四宽度滚动和等待动画、140 行分页与恢复通过。
- [x] Windows 500000 行导出回读哈希一致、500001 行超限拒绝、另一套 Windows 连续 3 次安装覆盖通过。
- [x] 完整备份并替换本机最终版；原 10 个数据库逐表不变，当前 60 行商品文件恢复及再次启动通过；Windows 安装包交付目录已更新。

以下为历史各版范围和验收记录。1.2.5 曾发现的锁冲突已在 1.2.6 修复，其历史失败记录保持原样，不代表当前版本仍有该阻断项。

## 2026-09-22 Windows 测试补充（仅测试）

- [x] Windows 原生构建、安装升级与数据保留、实际窗口滚动和分页已验证，详情见 [测试记录](windows-test-1.2.5-20260922.md)。
- [x] 1.2.5 曾有 SQLite 并发 100 次中 6 次失败，锁等待设置晚于首条数据库访问。已在 1.2.6 修复并以最终 EXE 100 次并发全部通过复验；1.2.5 历史失败不改写。
- [x] 500000 行完整修改/撤销/导出回读一致、500001 行超限拒绝通过；同一候选包在另一套 Windows 环境中连续 3 次安装与启动通过。
- 本次只推送测试分支和保留 Actions 测试产物，没有在线发布或改动本机安装。

## 1.2.5 商品转表滚动修复

- [x] 修复鼠标位于商品复核表格时上下滚动被吞的问题；表格只约束横向滚动，纵向滚动传给整页。
- [x] 使用《华住 9.18.xlsx》60 行原表，验证两种主题 × 1440/1024/842/390 宽度下向下、向上、连续小步滚动与最后一行可达；窄窗横向滚动保留。
- [x] 用 140 行两页样本验证实际滚动到分页控件并切到第二页。滚动前后会话行内容及业务工作区不变。
- [x] 原样式下稳定复现失败，修复后专项浏览器通过；411 项单元/集成测试及语法和运行清单检查通过。
- [x] 两平台成品专项验证、本机完整备份替换、原复核会话恢复与实际窗口滚动验证通过，详见 [1.2.5 交付记录](release-1.2.5-internal.md)。

## 1.2.4 已确认调整

- [x] “投入与利润”与“历史账本”合并为经营记录，共用日期范围；汇总、趋势、每日记录采用一致口径。未记录与真实零值区分显示。
- [x] 总账按计划或店铺比较，支持利润、亏损优先、消耗排序及明细下钻；ROI 使用总成交金额 / 总广告消耗，已更正、作废、旧试算不计入汇总。
- [x] 记录当天经营支持直接填写成交金额或支付 ROI，允许零广告有成交；退款、材料、运费与规格仍按完整原有规则核对并冻结保存。
- [x] 移除商品规格里的“另存定价方案”按钮；保留规则选择、单规格改价、保存尺寸组合，以及可复用规则里的方案维护。
- [x] 保留 1.2.3 的销售文件拖入、按尺寸合并、一键添加缺失尺寸及原子保存撤销；商品转表按最新 29 列标准，两列条码留空。
- [x] 打包前审查：411 项测试、600 组计算组合、真实销售表端到端、旧业务数据只读计算对比、保存失败重试、更正原值和备份恢复均通过。
- [x] 最终两个平台安装产物审计、包内真实流程复验、本机完整备份替换、数据逐字段比对与安装包交付完成。

最新详细记录见 [1.2.4 交付记录](release-1.2.4-internal.md)。历史验收证据保留如下。

## 1.2.3 增量交付

- [x] 销售表识别出明确尺寸、当前计划没有时，提供“一键添加此尺寸”，同时保留人工映射和排除。
- [x] 新规格继承计划有效默认材料、厚度、运费与定价方式；复用尺寸库中相同尺寸，长宽互换视为相同。默认规则无效时提示先修正，不猜测成本。
- [x] 点击后仅为“待添加”；点击“应用订单占比”才将尺寸、规格、销量、占比一起保存。取消添加、改为排除、放弃导入均不留下额外规格。
- [x] 防重复添加，保存失败可重试；撤销恢复原规格，新建尺寸未被其他计划或尺寸组合使用且未修改时一并移除。拖入文件保留。
- [x] `npm test` 400/400、`npm run check`、源代码及最终两个平台包内的真实销售表浏览器验证通过。本机已备份并换为 1.2.3；Windows 安装包已生成。Windows 包内验证在 Mac 上执行，Windows 原生安装仍待验证。

最新交付与证据见 [1.2.3 交付记录](release-1.2.3-internal.md)。以下基础范围沿用 1.2.2；其测试数量、性能及产物记录为当时的实际验证，不冒充本轮重跑。

## 本次范围

| 范围 | 实现与本次验收 |
| --- | --- |
| F01–F06 | 计划管理、规格列偏好、可复用规则、三类定价、手动价格保护、尺寸组合已接入正式数据与备份 |
| F07 | 销售导入改为按尺寸合并不同图案；保留拖入/点击/键盘选择，复核后回填销量与订单占比 |
| F08–F09 | 转表材质/厚度/尺寸复核与批量处理、G62 导出修复、最新 29 列表头与拖入文件 |
| F10–F15 | 包装字段兼容、指标文案、收起设置、上架价格计算器、店铺选择与经典计划列表、规则删除恢复 |
| F16 / UI-U01 | 侧光/日间主题；右上角在主题按钮旁加入“动效：开/关”，偏好持久化且遵循系统减少动态设置 |
| F17 | 受限文件子进程、SQLite 会话、流式读取/导出、100 行分页、50 万业务行边界 |
| 后续增量 | 生产尺寸统一、计划置顶/删除、退款后实际到手销售金额、混合年代旧账升级兼容 |
| 国内更新源 | 构建与运行时共用固定 OSS 地址，忽略旧 GitHub 环境变量，不自动下载、不自动安装、不降级 |

## 商品转表最新合同

以用户《商品转表(1).xlsx》为准，保留 29 列与顺序：

序号、平台、店铺、平台商品名称、平台规格名称、品牌、商品标签、尺寸、面积、宽、长、重量、成本、广告费、成交金额、主条码、规格辅助码、平台商品ID、平台规格ID、平台售价、售卖状态、平台库存、规格类型、规格名称、货品编码、货品简称、货品名称、商家编码、规格简称。

F 材质→品牌；G 厚度→商品标签；N 推广费→广告费；O 销售额→成交金额；P/Q 主条码与规格辅助码全部留空，不生成条码。29 列之外不新增输出。兼容旧表头输入；旧会话导出也按最新表头并清空两列条码。长编号保持文本，不改成本、重量与复核语义。

## 销售导入最新合同

- 默认读取“商品成交订单数”；“商品成交件数”仅在明确切换并确认一单一件估算后使用。不是独立订单号去重。
- 从 SKU 标题识别尺寸并统一单位，长宽互换视为相同尺寸；合并所有图案。存在多个平台、店铺或商品来源时先选择一个来源。
- 无尺寸、多尺寸、计划内重复尺寸需要人工映射或排除；计划缺失的尺寸需明确确认本次按 0。分母为已纳入数量，按百分之一分配舍入余数使合计 100%，全零拒绝。
- 识别出的明确尺寸在计划中缺失时可一键暂存新增规格；无尺寸、多尺寸、计划内重复尺寸不自动新增。应用时新增规格与销量、订单占比、来源记录一起保存，不覆盖已有售价和原商品/SKU 编号；支持撤销。排除记录计数按尺寸组保存。
- 暂时关闭保留本次运行中的复核，未应用时拦截退出/重启；“放弃本次导入”释放保护。未应用的选择不承诺跨重启恢复。
- 工作区/计划/尺寸变化拒绝旧候选；销毁后的迟到响应不得污染新窗口；同一导入编号的不同内容不能误报已保存。

## 1.2.2 基础版审查修复和证据

修复了数量中错误逗号被删除后误计销量、复核关闭后的退出保护遗漏、异步候选/保存响应污染重开窗口、排除组数丢失、同一导入编号内容变化误报成功。更新失败保留规范化的断网、连接重置、附件缺失与校验失败原因。

- [x] `npm test`（395/395）、`npm run check`、`git diff --check`。
- [x] 原始销售表实际浏览器验证：19 条记录→5 个尺寸、488 单；48.77%、27.46%、17.42%、3.69%、2.66%；拖入、点击、回填、保存、刷新和撤销。
- [x] 新动效开关、偏好刷新、系统减少动态、两种主题在 1440/842/390 宽度验证。
- [x] 找到并只读验证《华住 9.18.xlsx》60 行真实源；缺失厚度先阻止导出，隔离测试明确选择后验证 61 行/29 列/无公式/文本 ID/模板样式；1/60/99/100/139 边界。
- [x] 最终安装产物审计、50 万行完整链路与超限拒绝、真实大表解析层、旧备份/历史完整性复验；平台与真实大表验证边界详见交付记录。
- [x] 生成 Windows NSIS 安装包，备份并替换本机 Mac 应用为 1.2.2，记录版本和校验值。

本轮日志与测试脚本：外层 `outputs/release-1.2.2-20260921/`。最终结果见 [1.2.2 交付记录](release-1.2.2-internal.md)。

## 国内更新与正式发布边界

固定地址：`https://mat-roi-workbench-updates-2026.oss-cn-shanghai.aliyuncs.com/updates/windows/`。

- [x] 国内地址写入构建与运行时共享配置，最终 Windows 包须复验 `resources/app-update.yml`。
- [x] 本地修订后续发布脚本与工作流：校验候选 EXE 的 SHA-512/大小；先上传并核验 EXE/blockmap，最后写无缓存 latest.yml。附件验证失败不更新清单。脚本默认只校验，显式 `--publish` 才联网写入；本轮未调用发布模式。
- [ ] 后续正式发布时设置仅该目录所需的 OSS 上传权限及 CI Secrets；本轮没有配置远端凭据。
- [x] 2026-09-22 在 GitHub Windows runner 完成候选包原生安装/覆盖、旧版检查/下载/重启升级及 OSS 地址保留；只测试、不发布。1.2.5 当时使用同源重建的安装器；1.2.6 已直接验证与交付字节完全相同的 EXE，并修复该并发重算阻断项。
- [ ] Windows x64/8 GiB/SSD 上补齐最终产物性能与 Excel/WPS 验收，再允许正式在线发布。

首次从旧 GitHub 更新源切换须用新版 Windows 安装包手动覆盖一次。当前内测包不上传，因此 1.2.2 至 1.2.6 附件均未上传 OSS；禁止降级可避免较旧清单覆盖内测版。macOS 仍为手动安装，不宣称支持自动更新。
