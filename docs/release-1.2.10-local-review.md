# 1.2.10 本机检查版

本机检查阶段（2026-09-23）：已替换 `/Applications/地垫工作台.app`，版本与启动已确认；Windows 未构建，未推送或发布。

手动调整以商品链接为单位，默认收起，展开查看并逐条修改 SKU。每个商品可统一修改材质和厚度，支持只改厚度、事务保存、失败重试与撤销。搜索仍覆盖整表，搜索和 SKU 分页不会缩小整商品修改范围。导出前摘要显示实际厚度，存在不同厚度时明确标注。

验证：437 项测试全部通过，语法／运行清单／差异空白检查通过。最终 Mac 应用的真实窗口通过 120 SKU 跨页、同名商品隔离、批量修改与撤销、单条厚度例外、导出成本回读、搜索及恢复、两主题四宽度的布局和滚动验证；载荷、子进程与深层签名检查通过。

本机替换前已正常退出并校验 156 个备份文件。业务工作区内容和 10 个数据库完全保留；启动检查期间同一 334 行表重新导入，只有当前会话 ID／创建时间变化，其余数据表一致，没有回退当前操作。旧 1.2.9 应用复用现有已校验归档，当前 1.2.10 应用只展开在安装目录。

证据：`outputs/release-1.2.10-20260923/` 和 `outputs/mac-local-1.2.10-20260923/`。用户随后已确认并要求打包；Windows 已完成原生测试并交付，见 [内测交付记录](release-1.2.10-internal.md)。
