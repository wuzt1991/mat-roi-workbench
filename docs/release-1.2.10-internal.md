# 1.2.10 内测交付记录

2026-09-23，用户确认本机界面后要求打包。Windows 安装包已交付；Mac 保留用户已确认的 1.2.10，没有再次替换或重启。

## 本次交互

导入后先统一处理材质和厚度，再确认导出。手动调整按商品链接显示，默认收起，展开后查看不同 SKU；同商品可统一修改材质和厚度，保留单条编辑、全表搜索、撤销及拖入文件。整商品修改覆盖此商品所有 SKU，搜索和分页不改变作用范围。不同商品链接、店铺保持隔离。

## Windows 验证

测试地址：https://github.com/wuzt1991/mat-roi-workbench/actions/runs/35823293750

测试分支：`codex/windows-test-1.2.10-20260923`；提交：`6beda281f9f5e2f041c48cc3f76b39915fbc5065`。

- 本机构建前 437 项源码测试、语法及运行清单检查通过；同套源码测试在 Windows 工作流中通过。
- 最终交付 EXE 在 Windows 2022 runner 真实安装，旧版 1.1.10 检查、下载、重启升级到 1.2.10，旧工作区和混合年代记录保留。
- 安装后载荷与子进程审计、G62 的 1／60／99／100／139 行回归通过；29 列、原值、文本 ID、零库存及空条码得到检查。
- 实际安装窗口验证新材质厚度流程、商品分组和 SKU 分页、整商品修改及单条例外、保存失败保留输入、撤销、搜索范围、导出回读、重启恢复、两主题四宽度滚动和原等待动画。
- 安装包运行时 8 项锁与完成时序回归、100 轮并发测试全部通过，失败数 0。
- 500000 行完整导入、单条及整商品修改、撤销、导出回读通过。导出前后行哈希同为 `f05d8bf443779ad6a1326b26754166716f11dd4af82214bd5501656f61131196`；峰值内存 473.43 MiB。500001 行按边界拒绝。
- Windows latest runner 使用同一安装包连续三次安装覆盖、启动与版本健康检查通过。

测试使用隔离的合成数据。完整 UI 与升级报告保留在该 Actions 运行的 evidence artifact；公开 API 对这两份较长 annotation 截断至 4096 字节，本地保留原始截断文本及完整成功步骤、短升级摘要、大表和样本报告，不将截断文本冒充完整报告。

## 打包时修复

首次 Windows 运行在依赖安装阶段失败，定位到先前应用版本升级误改了构建依赖 minimist 的锁记录：原 1.2.8 被改成不存在的 1.2.9，本机已有完整性缓存掩盖了无效下载地址。空缓存安装稳定复现 E404 后，将版本、下载地址和上级范围恢复为原 1.2.8；重新安装和完整 Windows 流程通过。应用运行文件及生产依赖均未变化，原交付 EXE 的载荷审计继续通过。

## 交付文件与边界

安装包：`outputs/地垫工作台-1.2.10-内测/地垫工作台-1.2.10-windows-x64.exe`（外层工作区）。大小 103735088 字节。

SHA-256：`b41b39bebaa86b5536bbaaa76aa43b7ca131e442ea3040cb15c436ea02c65ea3`。

交付文件、提交给 Windows 测试的候选和升级时实际使用的 EXE 校验值一致。文件夹同时提供内测说明和 SHA256.txt。

仅推送独立测试分支；没有推送生产分支、创建标签或 Release、上传 OSS，也没有改动在线更新清单。未进行人工 Excel/WPS 验收或指定用户硬件性能验收，这些仍属于正式发布前的后续项目。

本轮本地证据：外层 `outputs/release-1.2.10-20260923/`，汇总为 `windows-final-verification.json`。Mac 本机替换记录沿用 `release-1.2.10-local-review.md`。
