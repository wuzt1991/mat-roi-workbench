# 历史方案：1.2.7–1.2.8（已由 1.2.9 替代）

更新：2026-09-23。本轮在自动转表基础上增加按材质预设厚度和整表商品搜索，本机已替换为 1.2.8，等待用户检查。保留此前已交付施工内容；只使用隔离 Windows 测试分支，不在线发布或推送更新。

## 本次施工：商品转表默认自动处理（2026-09-23，功能已落地）

以用户最新要求为准：完整自动识别时保持一键导出，不默认显示复核表格、材质／厚度／尺寸选项或字段配置；用户主动选择“手动调整”才展开完整编辑界面。

- [x] 拖入或选择 Excel 后自动识别。完整识别并通过数据校验时，直接显示文件概况与“一键导出 Excel”，省去“使用这次导入”和逐项确认；等待动画及拖入能力保留。
- [x] 不能完整识别时才弹出补充面板，只显示缺失或有歧义的项目。例如只缺厚度就只选厚度；只有一条尺寸有歧义就只处理该条；只有规格列不确定就只确认该列，提供真实内容样例。
- [x] 已识别的内容保留，不要求重新选择。同一商品共有的缺失项可确认一次并明确作用范围；不同规格仍需分别确定的内容不得静默覆盖。
- [x] 补齐后重新校验，全部满足条件后回到简洁的一键导出界面。导出完整文件；不得静默跳过未识别行、使用未经用户设置的默认值猜测缺失信息或把未计算值填成 0。
- [x] “手动调整”为独立可选入口，允许检查和修改已识别结果；修改即时保存与校验，点击“返回自动处理”回到导出概况。新流程不得丢失当前复核数据或主动覆盖单独修改过的值。

本次代码审查补充的验收条件：

- [x] 异常定位查询覆盖整份文件，包括第 101 行以后；只取当前需要处理的行，不把 50 万行加载到界面。
- [x] 同商品补填只处理真正缺失、相同材质的字段；保留自动识别、冲突和人工值；补材质后重新判断厚度。
- [x] 多张有效工作表必须明确选择；只有一张有效商品表时自动继续。列选择提供有限的真实样例，已确认列映射保持不变。
- [x] 新导入先完成读取和校验，再接纳；未完成旧会话替换前提示；失败、取消及未接纳候选不得覆盖旧恢复记录。
- [x] 缺少原表必填值不猜测；原表错误与可复用规则错误给出具体修正位置。待计算重量和成本显示空值，不显示 0。
- [x] 规则或识别校验版本变化阻止直接导出；重新校验保留人工选择，失效规则转为待处理。
- [x] 手动调整保留分页、全组编辑及滚动；退出手动后回到简洁概况。刷新恢复默认自动界面及已保存复核。
- [x] 切换工作区后的迟到响应丢弃；保存中防重复提交；失败保持输入，允许重试。
- [ ] 最终成品运行自动完整、仅缺厚度、单条多尺寸、仅规格列歧义、多工作表、失败替换、重载恢复、拖放和原版等待动画验收；交付同一 EXE 完成原生 Windows 测试。

交互示意：`/Users/wuzt/.codex/visualizations/2026/09/20/01a0be04-b5f2-7103-8b9f-fb9024796844/product-transfer-auto-first.html`。自动处理已在 1.2.7 落地；本轮 1.2.8 合并下面新增需求，重新验证最终成品。

## 1.2.8 新增：材质厚度预设与商品搜索

- [x] 上传区上方提供简洁厚度预设入口，可按材质选常用厚度，并按工作区记住下次导入。
- [x] 同材质跨不同商品的缺失厚度自动补齐；原表已有厚度、冲突值和单独人工修改保留。不预设的材质保持原识别流程。
- [x] 导出前可修改预设并应用到当前整表；预设与派生结果同批生效，失败或取消不发布半成品，人工例外值不被覆盖。
- [x] 商品名称、商品 ID、SKU、规格全文搜索覆盖整份表格，结果分页展示；搜索直接进入手动调整，清除搜索恢复全表。
- [x] 搜索后可单独改一条厚度；搜索不会改变导出范围，仍导出完整表格，整表存在未处理项时继续禁止导出。
- [x] 实际 Mac 成品验证多材质、多商品预设、原表厚度保留、跨页搜索单条修改、预设更换保留例外、失败重试、重启记忆；搜索后仍导出整表。
- [ ] Windows 最终 EXE 原生验证：等待用户检查本机后明确继续。
- [x] 备份 154 个文件并替换本机；11 个数据库逐表一致，334 条当前商品会话恢复。
- [ ] 用户验收本机后再继续 Windows 测试与正式内测包交付。

用户最新执行顺序（2026-09-23）：先替换本机供检查，确认后再进行下一步。1.2.8 本地候选已生成但未交付；测试工作树未提交、未推送，未创建线上 Release。

