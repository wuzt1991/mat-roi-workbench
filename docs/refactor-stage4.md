# 阶段 4：清理与成品验收

2026-09-23。基线 `ed25a91657ec600a9f6bbb89ccad043489f7f729`；候选版本 1.2.13。用户明确要求继续阶段 4。独立工作树施工，本机已安装 1.2.10 和原施工树保留；不推送、不发布、不运行远程 CI。仅使用合成隔离数据。

## 删除与保留

| 项目 | 依据和处理 |
| --- | --- |
| app.js 旧商品转表状态、规则适配、解析、导出、分页及复核处理 | 正式 index 已加载独立模块；正常运行不会调用，只有模块缺失才落到旧流程。删除并改为明确的模块不可用提示、保存保护下重新加载、备份入口 |
| app.js 旧投入与利润页面、演示弹窗、趋势指针监听及筛选状态 | 计划和总账使用 OperatingRecords；渲染路由没有旧 chartPage 入口。删除整个内部调用簇及只由它产生的动作 |
| app.js 无调用的规则视图包装和旧模块变量 | 词法引用核验后删除；保留仍被正式规则表单调用的包装 |
| workspace.css／responsive.css 旧 product-* 样式 | 只服务被删除的旧界面；共享 success、amber、分页和全局控件保留 |
| revision.css／workspace.css／ui-material-studies.css 旧 trend-*、investment-chart 样式 | 当前经营图表为 operating-*，旧渲染簇已删。混合选择器仅删过期部分，其他主题规则保持 |
| legacy-domain、domain-v3、workbook-v3、transfer-v3、trends-v3 | 当前初始化、旧账、迁移和工作簿仍依赖，保留；公式和数据格式不变 |
| product-transfer.js | 调用核验发现仅历史原型和兼容测试使用，源码保留；从正式 index 和运行时白名单移除，避免随成品携带无人调用的第二套转表实现 |
| 原型及历史视觉快照 | 不在运行时白名单；保留作历史参考，当前入口和维护文档明确替代关系 |

原有两项测试改为使用当前 canonicalRules、服务端规则快照和正式识别／人工确认接口，继续验证厚度或包边规则对应的成本与重量。旧测试隐式假定缺厚度可直接通过；当前批准流程必须显式确认，所以样本和操作按现行约定设置，不改变识别器。

## UI 审查

范围：被清理的外壳分支、旧样式以及新增模块缺失状态；检查 a11y、键盘、错误与布局、减少动态效果。现行销售、经营和商品页面用已有完整回归确认，本轮不重做视觉。

- `public/app.js`：`layout-empty-loading-error-states` / HIGH。旧回退静默切换到已废弃流程；现改为 `role=alert`、有名称的原生按钮、保存保护下重新加载和备份入口。新增针对测试及实际浏览器阻断模块请求复验。
- `public/product-transfer-ui.css`：`layout-long-content-safety`、`interaction-focus-visible` 复验通过；错误块独立作用域，长文本可换行，继承两主题变量。真实键盘 Tab／Shift+Tab 焦点可见。
- `public/workspace.css`、`responsive.css`、`revision.css`、`ui-material-studies.css`：仅清理已无渲染来源的选择器；共享按钮、表格、断点和主题级联保留。双主题、多窗口商品回归结果见最终证据。
- `public/sales-import-ui.css`、`operating-records.css`：未改动；特征前缀与 body 上的弹窗作用域保持，未强加失效的页面祖先选择器。
- Lattice 动画及减少动态效果行为未改写，使用原验收脚本复验。

## 资源与依赖

补齐 Lucide ISC 和 Inter OFL 完整许可证并加入同一运行白名单；ExcelJS MIT、React Bits 两份许可证保留。来源分别为 lucide 0.468.0 LICENSE、Inter v4.1 LICENSE.txt，资源文件本身未替换。生产依赖版本不变。20 个生产包均保留许可证元数据；其中 19 个包自带独立许可证文件，lazy-val 1.0.5 上游 npm 包仅声明 MIT、未附独立文件，本轮保留其原始元数据，不编造上游声明。

首次本机 Windows 构建因共享 node_modules 符号链接使 electron-builder 漏收传递依赖，被 afterPack 拒绝。未绕过审计；改用独立临时目录按锁文件安装生产依赖，再构建并与当前源码对照。失败日志保留。

## 验证结果

本阶段本地实施与验收已完成；Windows 原生项保留待验收。最终证据集中在工作树同级 `outputs/refactor-stage4-20260923/`，总索引 `final-verification.json`。

| 验证 | 结果 |
| --- | --- |
| 自动测试与语法 | 462 通过，0 失败；全部 JavaScript 及运行白名单检查通过 |
| 基线对照 | 23 组有效页面／弹窗 HTML 相同，计算和工作簿表内容相同 |
| 数据核心保护 | 24 个当前计算、历史模型、数据库、会话和文件服务模块与阶段 3 字节相同 |
| Mac 实际成品业务 | 规则失败保留草稿；销量按尺寸汇总、新尺寸暂存、保存失败重试及撤销；入账失败重试不重复、更正保留快照、总账有效版本；备份导出、合并恢复与重载通过 |
| Mac 商品回归 | 拖放、统一材质厚度、分组展开与 SKU 分页、搜索、商品批量与单条修改、响应丢失核对、撤销、整表导出；两主题 × 四窗口宽度滚动和页尾可达通过，Lattice 动画及动效设置通过 |
| 模块缺失与 UI | 实际阻断模块请求；两主题 × 390/1440 宽度无横向溢出；错误可见、键盘焦点、备份入口、Enter 重新加载及原数据一致通过；截图已目视核验 |
| Mac 升级与回退 | 实际 1.2.10 建立合成库 → 1.2.13 读取、保存、导出、替换恢复 → 实际 1.2.10 回读；全部业务字段和冻结账目一致，原已安装载荷未变 |
| Mac 成品与归档 | 89 个运行源文件哈希固定；载荷、原生子进程和签名通过；ZIP 全部 284 个文件、14 个符号链接、执行权限、系统实际解压及签名复验通过；含 Electron/Chromium 许可证 |
| Windows 成品静态 | NSIS 安装器本地构建成功，解开最终 EXE 得到 x64 应用；内嵌程序及 app.asar 与构建目录相同，运行白名单、生产依赖及每个应用文件与源码相符；未执行原生 Windows |
| 保护与清理 | 原施工树 292 个文件不变；已安装 1.2.10 哈希不变；候选仅留最终 ZIP/EXE、说明及验收证据，中间展开应用和构建目录已移除 |

最终 Mac ZIP：`../../outputs/refactor-stage4-20260923/candidates.noindex/地垫工作台-1.2.13-结构重构候选-Mac-arm64.zip`。SHA-256：`bd7e4ea66230176ffb4c4c8b87e7a95d00f214f5166622224efddeaedd6b1c8e`。

最终 Windows EXE：`../../outputs/refactor-stage4-20260923/candidates.noindex/地垫工作台-1.2.13-windows-x64.exe`。SHA-256：`b679331616d2abfb4b145a8292b1e543c0aab02c69c76a39926a05953abb6253`。

实际验收的 Mac app.asar：`655902c2fe6c210cde33878d452aa2887093c7620f032312e13cda6d09baddff`；Windows 内嵌 app.asar：`ed8c5fccb3ed16778407a22ce132a8471c99b6633bdd5116fefe052d81b0224d`。两个 Mac 业务报告、商品报告和升级回退报告对应同一最终载荷。

本轮排查：升级测试第一次主动导航与应用初次加载重叠而超时，调整为等待原生窗口自行完成导航；随后合成样本缺规格被正常校验拒绝，补充有效合成规格后通过。归档补齐运行库许可证时遇到 ditto 的 UTF-8 文件名标记差异，重新生成规范 Unicode 条目，并逐文件、系统解压及 codesign 复验。失败证据保留；未通过绕过数据校验、修改安装器或改变业务逻辑处理这些测试问题。

Windows 原生安装、升级、Excel/WPS 人工核验、8 GiB Windows 机器验证不以本机 Mac 构建替代。尚无本轮原生执行环境，不标通过。

维护入口见 `refactor-maintenance.md`。旧阶段报告保留历史结果，当前主清单按本轮结果更新。

## 后续本机替换

用户随后明确要求“先给本机替换了”。已将同一 Mac 候选安装到本机并完成真实工作区一致性核对，详见 `release-1.2.13-local-review.md`。上方“未替换本机”描述保留为阶段 4 验收时的历史边界。
